#!/usr/bin/env ruby

require_relative "../lib/site-inspector"
domain = ARGV[0]

raise "Usage: site-inspector [DOMAIN]" if domain.to_s.empty?

SiteInspector.new(domain).to_hash.each do |key, value|
  puts "#{key}: #{value}"
end
